# === Render Environment Variables ===

# Repository Info
RENDER_GIT_REPO_SLUG=Life-Experimentalist/CounterAPI
REPO_WIKI=https://github.com/Life-Experimentalist/CounterAPI/wiki

# Runtime Deployment Metadata (set automatically by Render, no need to override)
# RENDER_GIT_COMMIT=unknown
# RENDER_SERVICE_ID=unknown
# RENDER_SERVICE_NAME=unknown
# RENDER_EXTERNAL_URL=unknown
# RENDER_GIT_BRANCH=main

# === SQLite Database Path ===
DB_PATH=/data/counters.db

# === CORS (optional, allow all for now) ===
CORS_ALLOW_ORIGINS=*

# === Future Use (optional, placeholders for extensibility) ===
PROJECT_NAME=CounterAPI
DEFAULT_COUNT=0
